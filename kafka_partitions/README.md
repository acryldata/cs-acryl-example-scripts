# Kafka Partition Checker

Check the number of Kafka partitions for MCL and MCP consumer groups in a DataHub instance.

## Usage

```shell
# Ensure datahub CLI is configured
datahub init

# Run the script
python script.py

# Print full JSON response from the endpoints
python script.py --verbose
```
