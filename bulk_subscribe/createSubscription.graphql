mutation createSubscription($input: CreateSubscriptionInput!) {
    createSubscription(input: $input) {
        subscriptionUrn,
        actorUrn,
        subscriptionTypes,
        entityChangeTypes {
            entityChangeType, 
            filter {
                includeAssertions
            }
       }
    }
}